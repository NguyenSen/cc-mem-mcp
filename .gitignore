__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.env
# local embedded Qdrant data
.cc-mem/
data/
*.qdrant/
# realized hook wrappers hold machine-specific paths + your Qdrant URL; ship only *.example
hooks/*.cmd
hooks/*.sh
# realized eval runners hold your Qdrant URL; ship only *.example
eval/run.sh
eval/run.cmd
# fastembed model cache if it lands in-tree
.fastembed_cache/
